language: python

python:
    - "3.6"

services:
    - postgresql

install:
    - pip install -r requirements.txt

before_script:
    - source stackoverflow/.travis.sh
    - psql -c "create database stackoverflow_api;" -U postgres
    - psql -c "create user postgres with password 'shema';" -U postgres

script:
    - py.test --cov=StackOverflow-lite

after_success:
    - coveralls
